enum EMessage {
  NetworkError = "Sorry, apparently we have network issues. Try again, if the problem persists, try to connect later."
}

export default EMessage

export enum EUser {
  SUCCESS_SIGNUP = "Your account has been created, welcome to our community.",
  SUCCESS_LOGIN = "Welcome"
}

export enum EOrder {
  FAILED_CHECKOUT = 'Something went wrong. Please try again'
}